---
interface Props {
  class?: string;
  px?: string;
}

const {
  class: className = "",
  px = "px-4 md:px-8 xl:px-12 2xl:px-16",
} = Astro.props;
---

<div class={`mx-auto w-full max-w-360 ${px} ${className}`}>
  <slot />
</div>
